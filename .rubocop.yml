plugins:
  - rubocop-rake
  - rubocop-rspec

AllCops:
  TargetRubyVersion: 3.4
  NewCops: enable

# Disable because there are many api methods that start with get_
Naming/AccessorMethodName:
  Enabled: false

# Disable as some api params are short
Naming/MethodParameterName:
  Enabled: false